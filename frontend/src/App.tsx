import type React from 'react'

function App(): React.ReactElement {
  return (
    <div className="app-container">
      <h1>Rocket-Leaf</h1>
      <p>前端已重置为空 React 项目，可在此开始开发。</p>
    </div>
  )
}

export default App
